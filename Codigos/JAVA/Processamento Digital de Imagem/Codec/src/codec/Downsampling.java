package codec;

public class Downsampling {

    /*public YCbCr[] downsample (YCbCr[] img, int len, int n) throws IOException {

        YCbCr[] vec = new YCbCr[len];
        vec[0] = img[0];
        for(int i = 1; i < len; i+=(n-1)){
            vec[i] = img[i];
        }
        return vec;
    }*/

}
